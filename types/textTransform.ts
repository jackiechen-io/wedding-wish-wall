export type TextTransform = {
  x: number;
  y: number;
  rotation: number;
  fontSize: number;
};

export const DEFAULT_TEXT_TRANSFORM: TextTransform = {
  x: 50,
  y: 45,
  rotation: 0,
  fontSize: 56,
};
